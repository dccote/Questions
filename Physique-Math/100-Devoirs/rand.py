import numpy

# numbers = numpy.random.normal(loc=10.16, scale=2.54, size=1000)
# for n in numbers:
# 	print(n)
    
numbers = numpy.random.normal(loc=4, scale=1, size=1000)
for n in numbers:
	print(n)
